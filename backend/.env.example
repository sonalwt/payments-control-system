NODE_ENV=development
PORT=4000
API_PREFIX=api/v1

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=pcs
DB_SCHEMA=public
DB_SYNCHRONIZE=false
DB_LOGGING=false

# JWT
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=8h

# Employee self-service portal (passwordless OTP login)
EMPLOYEE_JWT_EXPIRES_IN=2h
EMPLOYEE_OTP_TTL_MINUTES=10
EMPLOYEE_OTP_MAX_ATTEMPTS=5

# CORS
CORS_ORIGIN=http://localhost:3000

# AWS S3 (document storage)
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
# Optional: set only for S3-compatible services (MinIO, LocalStack, etc.)
# AWS_S3_ENDPOINT=http://localhost:9000
# Public base URL of the frontend (used to build links in emails)
FRONTEND_URL=http://localhost:3000

# SMTP (password-reset emails). Leave SMTP_HOST empty to log the reset link
# to the server console instead of sending a real email (dev fallback).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=sonal@firsteconomy.com
SMTP_PASS=lhptwafkqbomwvbs
MAIL_FROM=Payments Control System <no-reply@pcs.local>

# Foreign Exchange Rates (§2.2). USD is the group reporting/base currency.
# Daily rates are fetched from the OANDA Exchange Rates API at 06:00 Dubai.
# Leave OANDA_API_KEY empty in dev: the daily fetch then holds the previous
# day's rate (source=STALE_HELD) and admins can set rates via manual override.
FX_BASE_CURRENCY=USD
FX_PROVIDER=OANDA
OANDA_API_URL=https://www.oanda.com/rates/api/v2/rates/spot.json
OANDA_API_KEY=
FX_REQUEST_TIMEOUT_MS=15000
